module.exports = {
    name: 'chrisisbad',
    description: 'You\'re wrong',
    category: 'Other',

    run: async (client, message, args, channel, tags, isMod, isOwner, settingsDir, channelName, username, prefix, send) => {
        // Just another ...isbad command
        send(`Take that back RIGHT NOW`)
    }
}